# SewershedDelineation — Python dependencies
#
# The geospatial stack (geopandas/shapely/pyogrio) installs most reliably from
# conda-forge; see README.md. Version floors below reflect the APIs the code
# actually uses. Validated on the exact versions listed at the bottom.

# --- Core geospatial ---
geopandas>=1.0        # served-unit selection, dissolve, GeoPackage/shapefile IO
shapely>=2.0          # 2.x API: union_all, concave_hull, STRtree.query(predicate=)
pyogrio>=0.7          # GDAL IO backend; used directly in run.py (list_layers)
networkx>=3.0         # directed sewer graph + reverse (upstream) traversal

# --- Scientific ---
numpy>=1.24
scipy>=1.10           # scipy.spatial — Voronoi equidistant split, KD-tree snapping
pandas>=2.0
matplotlib>=3.7       # zoomed PDF flag maps

# --- IO / config / web ---
pyyaml>=6.0           # config.yaml
requests>=2.28        # Census API — demographic-join phase only (run_demographics.py)

# --- Optional ---
contextily>=1.3       # satellite/street basemaps under PDF maps; skipped gracefully if absent

# ---------------------------------------------------------------------------
# Validated environment (2026-07-12):
#   python 3.13.12
#   geopandas 1.1.3   shapely 2.1.2   pyogrio 0.12.1   networkx 3.6.1
#   numpy 2.4.4       scipy 1.17.1    pandas 3.0.2     matplotlib 3.10.8
#   pyyaml 6.0.3      requests 2.32.5 contextily 1.7.0
# ---------------------------------------------------------------------------
